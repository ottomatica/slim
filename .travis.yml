language: node_js
os: osx
addons:
  homebrew:
    casks:
      - docker
      - vagrant
    packages:
      - gnu-tar
      - cdrtools
node_js: 8 
install: npm install && npm link
script: slim build images/alpine3.8-simple && slim images
