language: generic
os: osx
addons:
  homebrew:
    casks:
      - docker
      - vagrant
    packages:
      - gnu-tar
      - cdrtools
install: nvm use 8 && npm install && npm link
before_script: node --version
script: slim build images/alpine3.8-simple && slim images
